version: '3'
services:
  receiver-service:
    build: ./receiver-service
    ports:
      - "3000:3000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/recevier-service-db
      - REDIS_HOST=localhost
      - REDIS_PORT=6379
    depends_on:
      - mongo
      - redis

  listener-service:
    build: ./listener-service
    ports:
      - "3001:3001"
    environment:
      - MONGO_URI=mongodb://mongo:27017/listener-service-db
      - REDIS_HOST=localhost
      - REDIS_PORT=6379
    depends_on:
      - mongo
      - redis

  mongo:
    image: mongo
    ports:
      - "27017:27017"

  redis:
    image: redis
    ports:
      - "6379:6379"
